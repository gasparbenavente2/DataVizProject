data-pipeline/.venv/
data-pipeline/.env
data-pipeline/output/
.claude/
__pycache__/

# Exclude credentials and GCP keys (any .json not explicitly allowed below)
*.json
!app/package.json
!app/package-lock.json
!app/tsconfig.json
!app/public/world-110m.geojson
!vercel.json

# Pipeline output parquets stay out of git (large/binary)
# but app/public/data/ parquets are committed so teammates get the data
*.parquet
!app/public/data/*.parquet
*.csv
